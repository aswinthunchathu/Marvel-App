import { ISeriesList } from '../../shared/types'

export interface IProps {
    characterId?: string
}

export interface IVariables {
    offset?: number
    limit?: number
    characterId?: string
}
